# FastAPI Social Media Backend

A RESTful social media backend API built with **FastAPI**, featuring JWT authentication, a PostgreSQL database, and a full Dockerized deployment pipeline. This is a from-scratch backend project covering the core patterns of a production-style API: relational data modeling, authentication, database migrations, testing, and CI/CD.

**Live API docs:** `https://fastapi-social-media-latest.onrender.com/docs`

---

## Features

- **User accounts** — registration and retrieval, with passwords hashed via `pwdlib`
- **JWT authentication** — OAuth2 password flow (`/login`), bearer tokens required on protected routes
- **Posts (CRUD)** — create, read, update, delete, with ownership checks (only the author can edit/delete their post)
- **Likes / dislikes** — toggle-style voting on posts, with live like/dislike counts aggregated via SQL joins
- **Search & pagination** — filter posts by title, with `limit`/`skip` query params
- **Alembic migrations** — versioned schema changes instead of `create_all()`
- **Automated tests** — pytest suite covering users, posts, and voting
- **Dockerized** — separate dev (`docker-compose.yaml`) and prod (`docker-compose-prod.yaml`) configurations
- **CI/CD** — GitHub Actions pipeline: spins up a Postgres service container → runs tests → builds a Docker image → pushes to Docker Hub → triggers a Render deploy hook

## Tech Stack

| Layer          | Technology                          |
|----------------|--------------------------------------|
| Framework      | FastAPI                              |
| Database       | PostgreSQL (hosted on [Neon](https://neon.tech)) |
| ORM            | SQLAlchemy 2.0                       |
| Migrations     | Alembic                              |
| Auth           | JWT (PyJWT) + OAuth2PasswordBearer   |
| Password hashing | pwdlib                             |
| Testing        | pytest                               |
| Containerization | Docker, Docker Compose             |
| CI/CD          | GitHub Actions → Docker Hub → Render |

## Project Structure

```
app/
├── main.py           # FastAPI app instance, router registration, CORS
├── database.py        # SQLAlchemy engine/session setup
├── models.py           # ORM models: User, Post, Like
├── schemas.py          # Pydantic request/response schemas
├── configs.py           # Environment-based settings (pydantic-settings)
├── utils.py              # Password hashing helpers
├── Oauth2.py               # JWT creation/verification, current-user dependency
└── routers/
    ├── auth.py               # POST /login
    ├── user.py                 # User endpoints
    ├── post.py                  # Post CRUD endpoints
    └── like.py                   # Like/dislike endpoint

alembic/                # Migration environment and versioned migrations
tests/                  # pytest test suite
Dockerfile
docker-compose.yaml         # Local development
docker-compose-prod.yaml     # Production
entrypoint.sh                  # Runs Alembic migrations, then starts uvicorn
.github/workflows/               # CI/CD pipeline
```

## API Overview
Response format:
JSON

Authentication:
Bearer Token

| Method | Endpoint         | Description                          | Auth required |
|--------|------------------|---------------------------------------|:--------------:|
| POST   | `/users/`        | Create a new user                     | No |
| GET    | `/users/{id}`    | Get a user by ID                      | No |
| POST   | `/login`         | Log in, receive a JWT access token    | No |
| GET    | `/posts/`        | List posts (search, pagination, like/dislike counts) | Yes |
| POST   | `/posts/`        | Create a post                         | Yes |
| GET    | `/posts/latest`  | Get the most recently created post    | Yes |
| GET    | `/posts/my_posts`| Get posts created by the current user | Yes |
| GET    | `/posts/{id}`    | Get a single post with like/dislike counts | Yes |
| PUT    | `/posts/{id}`    | Update a post (owner only)            | Yes |
| DELETE | `/posts/{id}`    | Delete a post (owner only)            | Yes |
| POST   | `/likes/`        | Like/dislike a post (toggles on repeat) | Yes |

Full interactive documentation (Swagger UI) is auto-generated by FastAPI and available at `/docs` on the running server.

## Running Locally

### With Docker (recommended)

```bash
git clone https://github.com/Abhishekkjcodes/FastAPI_Backend_Project.git
cd FastAPI_Backend_Project
docker compose up --build
```

The API will be available at `http://localhost:8000`, with docs at `http://localhost:8000/docs`.

### Without Docker

1. Create a PostgreSQL database.
2. Create a `.env` file in the project root:
   ```
   DATABASE_HOSTNAME=localhost
   DATABASE_PORT=5432
   DATABASE_NAME=fastapi
   DATABASE_USERNAME=postgres
   DATABASE_PASSWORD=<your_password>

   SECRET_KEY=<your_secret_key>
   ALGORITHM=HS256
   ACCESS_TOKEN_EXPIRE_MINUTES=30
   ```
3. Install dependencies and run migrations:
   ```bash
   pip install -r requirements.txt
   alembic upgrade head
   ```
4. Start the server:
   ```bash
   uvicorn app.main:app --reload
   ```

## Running Tests

```bash
pytest -x
```

Tests run against a separate test database, configured via `TESTING_*` environment variables (see `app/configs.py`).

## Deployment

The app is deployed on [Render](https://render.com), with the database hosted on [Neon](https://neon.tech). On every push to `main`, GitHub Actions runs the test suite, builds a Docker image, pushes it to Docker Hub, and triggers a Render deploy hook.

### Deployment Flow

GitHub Push
      ↓
GitHub Actions
      ↓
Pytest
      ↓
Docker Build
      ↓
Docker Hub
      ↓
Render Deploy Hook
      ↓
Render
      ↓
Neon PostgreSQL

## Notes

This project was built to explore backend development concepts including REST API design, authentication, relational databases, automated testing, containerization, and CI/CD deployment.

Future improvements include refresh tokens, role-based authorization, caching (Redis), rate limiting, and API versioning.